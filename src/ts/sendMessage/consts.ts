const errorMessages = {
	elementNotFound: ' - not found. Ensure that you have specified the class or defined the element correctly',
};

const empty = '';
const colon = ':';

export {errorMessages, empty, colon};
